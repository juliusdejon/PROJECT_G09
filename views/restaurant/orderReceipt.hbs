<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: 'Inter', sans-serif;
            font-size: 14px;
        }
        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .item-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 50%;
            margin: 0 auto;
            border: 1px solid black;
            border-radius: 10px; 
            gap: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Order Confirmation Code: {{orderCode}}</h2>
        <h3>Order total: ${{orderTotal}}</h3>
        <h3>Items: </h3>
        {{#each orderItems}}
        <div class="item-container">
            <p>Name: {{name}}</p>
            <p>Price: {{price}}</p>

        </div>
        {{/each}}
    </div>
</body>
</html>